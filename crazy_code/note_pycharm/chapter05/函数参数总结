函数的参数
    实际参数
        1、位置实参：按形参顺序传入
        2、关键字实参：按形参参数名传入
        注：关键字实参必须位于位置实参后面。

        3、逆向参数收集:将列表、元组、字典的元素拆开后传给函数的参数。
            序列逆向收集
                需要在列表、元组之前添加一个*号
                即使传给支持收集的参数也需要使用逆向收集
            字典逆向收集
                需要在字典参数前面添加两个*号
                字典以关键字实参的形式传入。

    形式参数
        1、位置形参：必须传入实参
        2、默认形参：可以不传入实参
        注：默认形参必须在没有默认的形参后面。在形参列表最后。

        3、个数可变的形参（参数收集）：
            a （收集位置实参的）个数可变形参
                形参前添加一个*号，该参数可接收多个参数值，以元组传入。
                可以在形参列表的任意位置,一个函数最多只能带一个支持"普通"参数收集的形参。
            b （收集关键字实参的）个数可变形参：
                将关键字参数收集成字典。
                参数前面添加两个*号。
                一个函数可同时包含一个支持"普通"参数收集的参数和一个支持关键字实参收集的参数
